server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rupeshbhondave1681/paywave-lite
          clone-on-start: true
          default-label: main

     
